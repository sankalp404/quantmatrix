# Test-safe environment variables for pytest/CI (do NOT use production .env)
TEST_DATABASE_URL=postgresql://quantmatrix:quantmatrix@postgres:5432/quantmatrix_test
# Optional: point DATABASE_URL to a dev DB when running tests locally. Avoid prod.
# DATABASE_URL=postgresql://quantmatrix:password@postgres:5432/quantmatrix_dev
SEED_ACCOUNTS_ON_STARTUP=false

